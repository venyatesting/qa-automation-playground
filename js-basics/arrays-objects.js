const skills = ["manual testing", "qa testing processes", "automation in progress"];
skills.push("JavaScript");

const profile = {
  name: "Иван",
  age: 30,
  skills: skills
};

// Выводим массив и объект
console.log(skills);
console.log(profile);
